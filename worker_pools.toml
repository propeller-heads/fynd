# Worker pools configuration file

# Worker pools
[pools.most_liquid_2_hops]
algorithm = "most_liquid"
num_workers = 10
task_queue_capacity = 1000
max_hops = 2
timeout_ms = 1000

#[pools.most_liquid_3_hops]
#algorithm = "most_liquid"
#num_workers = 1
#min_hops = 2
#max_hops = 3

# Add more pools by adding entries under [pools.*], e.g.:
# [pools.dijkstra]
# algorithm = "dijkstra"
# num_workers = 8
# task_queue_capacity = 1000
# min_hops = 1
# max_hops = 5
# timeout_ms = 200
