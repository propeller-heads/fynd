// Should use the TychoEncoder from tycho-execution and an alloy provider to execute the txs
// (see the quickstart for an  example)
pub struct Executor {
    // encoder: TychoEncoder,
    // provider: Alloy Provider
}

impl Executor {
    pub fn execute() {
        println!("Encoding and executing the trade");
    }
}
